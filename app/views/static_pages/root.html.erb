<% if logged_in? %>
    <script type='text/javascript'>
        window.currentUser = <%= render(
            '../api/users/_user.json.jbuilder', 
            user: current_user).html_safe %>
        console.log(window.currentUser)
    </script>
<% end %>

<div id='root'>☠️ Something went wrong ☠️</div>